
current:

 - create intro

misc:

 - when selecting slice in search - make it highlighted
 - implement multiple documents slices editing (all must have the same slices only)
	- ability to add new slice to all selected documents at once
 - switch note editor to https://github.com/flyween/vue3-quill
 - transact edit/delete operations in DB that consist of several steps
 - add div zoom by scroll/pan event to slices
 - create main Slices component (reuse it in sliceselect)
 - add to file:
	  - collection flag (add entries/directories)
			- play collections
			- edit collections, implement skip flag for entry
		- streaming play of media files (videos)
 - notify options:
   - notify me before
 - create sfinx telegram webapp (https://core.telegram.org/bots/webapps)
 - make viewdocuments as readonly versions of newedit dialog with hidden edit button ???
 - add voice search (read the book)
	https://developer.mozilla.org/ru/docs/Web/API/Web_Speech_API/Using_the_Web_Speech_API
 - add static colors to slices
 - write automated tests (puppeteer based)
 - connect ebook reader and save reading history in DB (pages, annotations, etc.) for PDF/EPUB/DJVU/TXT formats
 - implement file content encryption upon upload
 - ability to import files from local directory (electron or app or chrome extension)
 - ability to work with several documents at once (q-window ?)
 - add mobile browser layout

next:

 - add expose feature for slices by sfinx:// URL (permissions / credentials) / add "remote connected" option to slices / integrate search queries
	- add public API endpoint to sfinxd
 - implement slice semantics (json-ld)
 - implement basic AI agents:
    - watcher
    - harvester
 - telegram:
	- export slice as channel

